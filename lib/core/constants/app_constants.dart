class AppConstants {
  const AppConstants._();

  static const appName = 'Trader Ledger App';
  static const fullAppName =
      'Trader Ledger App (Manage Bills, Payment, deposite & reports)';
  static const maxEntryImages = 15;
}
